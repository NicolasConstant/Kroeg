{
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "Kroeg": {
    "TokenSigningKey": "Test key, please ignore",
    "RewriteRequestScheme": false,
    "CanRegister": true,
    "FileUploadPath": "./wwwroot/images/",
    "FileUploadUrl": "http://localhost:5000/images/",
    "UnflattenRemotely": true,
    "BackgroundThreads": 1,

    /*
      You can add custom modules that contain classes that extend BaseHandler, to modify behaviour of the server, without recompiling the whole thing
      (e.g. spam filter, limit character count). This is ran on *all* incoming messages, both inbox and outbox, and won't stop external objects from
      creeping into the DB (e.g. manually looking up the object) - but it *will* stop them from showing in the inbox.
    */
    "Modules": {
//      "first": "../Kroeg.TestFilter/bin/Debug/netcoreapp2.0/Kroeg.TestFilter.dll"
    },
    "Filters": {
//      "first": "Kroeg.TestFilter.SpamFilter"
    }
  },
  "EntityNames": {
    "fallbackCategory": "${$.attributedTo|$.actor|resolve|%.preferredUsername|shortguid}/${$.type|slug}/${shortguid}",
    "activityCategory": "${$.attributedTo|$.actor|resolve|%.preferredUsername|shortguid}/status/${shortguid}",
    "objectRelative": "+${$.summary|$.content|$.type|slug}",

    "actorCategory": "users/${$.preferredUsername|guid}",

    "followingCategory": "+following",
    "followersCategory": "+followers",
    "likesCategory": "+likes",
    "likedCategory": "+liked",
    "outboxCategory": "+outbox",
    "inboxCategory": "+inbox",
    "blocksCategory":  "+blocks",

    "sharesRelative": "+shares",
    "repliesRelative": "+replies"
  },
  "ConnectionStrings": {
    "Default": "Host=localhost;Username=postgres;Password=postgres;Database=kroeg4"
  }
}
